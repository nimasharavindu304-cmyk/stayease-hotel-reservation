# StayEase database configuration
# Update these values to match your local MySQL installation.
db.url=jdbc:mysql://localhost:3306/stayease_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
db.user=root
db.password=+a#NR2o!8Ze#68Kr12ZeKKv@

# Optional email integration (booking confirmation).
# Leave smtp.enabled=false if you do not want to configure this.
smtp.enabled=false
smtp.host=smtp.gmail.com
smtp.port=587
smtp.username=your-email@gmail.com
smtp.password=your-app-password
